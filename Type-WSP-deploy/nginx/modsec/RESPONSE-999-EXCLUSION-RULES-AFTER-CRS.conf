# ======================================================
# 在 OWASP CRS 套用後執行的規則
# 用途:觀察到實際誤判後,精準放行特定規則 ID
# 部署後請查看日誌: docker logs nginx-waf 2>&1 | grep "ModSecurity"
# 發現誤判後用 ctl:ruleRemoveById 排除,勿整體關閉
# ======================================================

# (範例模板,待上線後按實際誤判補上)
# SecRule REQUEST_URI "@beginsWith /api/posts" \
#     "id:2001,\
#     phase:1,\
#     pass,\
#     nolog,\
#     ctl:ruleRemoveById=920230"
